# uBuilder
A lightweight tool to design and generate embedded software for microcontrollers.

First version of uBuilder is capable to design and generate both a Memory Access Layer and a Registry Access Layer (in C language) for a given microcontroller. The tool is also capable to generate code for a virtual (emulated) target running on a desktop PC rather than the real hw.
